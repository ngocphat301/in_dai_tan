- content_for :title, "Danh mục blog — mới"
h1 Danh mục blog mới
= render "form", blog_category: @blog_category
p style="margin-top:1rem"
  = link_to "← Danh sách", admin_blog_categories_path, class: "btn ghost"
